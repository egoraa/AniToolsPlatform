# Точка продолжения: платформа исполнения (пайплайн/группы/раннер)

Дата: 2026-07-11. Задача: концепт и план реализации платформы исполнения модулей.

## Где мы

Брейншторм завершён, дизайн одобрен по секциям, спека написана и обновлена
до финальной модели потоков (пул + назначения). Self-review пройден.

- Спека: `.superpowers/specs/2026-07-11-pipeline-platform-design.md`
  (пользователь скопировал её же в `docs/2026-07-11-pipeline-platform-design.md`;
  на момент записи копии идентичны — при правках синхронизировать или выбрать одну).
- Ревью спеки пользователем: **не завершено** — он просмотрел обновление после
  смены модели потоков, явного «ок, спека финальна» ещё не было.

## Следующий шаг

1. ~~Подтверждение спеки~~ — получено («продолжим»).
2. ~~writing-plans~~ — план готов: `.superpowers/plans/2026-07-11-pipeline-platform.md`
   (10 задач, TDD). В спеку при планировании добавлено: виртуальные
   `module_base::inputs()/outputs()` (ковариантный override в `module<>`) и
   `plugin_abi` → 3 — без них connect по именам через `module_base` невозможен.
   Копия спеки в `docs/` после этого отстала — напомнить пользователю.
3. Исполнение плана: выбрать subagent-driven (superpowers:subagent-driven-development)
   или inline (superpowers:executing-plans). Коммиты — только пользователь,
   в конце каждой задачи предлагать сообщение.

## Ключевые решения (кратко; полная версия — в спеке)

- Три сущности: `group` (структура: владение модулями/подгруппами, экспорт портов
  как алиасы, connect по путям) + `pipeline` (корневая группа + service_directory +
  module_context) + `pipeline_runner` (пул потоков, жизненный цикл).
- Потоки: пул из N `std::jthread`; `assign(group&, index)`; несколько групп на
  поток — норма, цикл потока по кругу; группа без назначения — inline в цикле
  ближайшего назначенного предка (в позиции своего порядка вставки); корень без
  назначения → поток 0; пустая конфигурация = однопоточный пайплайн.
- Проверка `safe`-входов: критерий — граница потоков (не групп), выполняется
  раннером на шаге 1 `start()` до каскадов; записи соединений в группе хранят
  группы-владельцы сторон.
- Ошибки: initialize/start — fail-fast с откатом в обратном порядке; iterate —
  первый exception_ptr побеждает, request_stop всему пайплайну, `wait()`
  перебрасывает, `error()` возвращает.
- Раскладка: новые классы в `src/atp/` (header-only, цель `atp_host` INTERFACE
  поверх `atp_platform`); `module_loader.hpp` переезжает из `include/` в `src/`;
  в `include/` — SDK автора модулей + один аксессор `io_base::thread_safe()`.
- `group::make<M>()` реализует отложенный «вариант A» владеющего реестра
  (память `module-registry-variant-a`).

## Напоминания

- Коммиты — только пользователь, самим не коммитить.
- Спеки/планы — в `.superpowers/` (gitignored); постоянная документация — `docs/`.
- Комментарии в коде — по-русски, стиль — `docs/code_style.md`.
