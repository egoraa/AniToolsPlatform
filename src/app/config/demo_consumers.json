{
    "group": "consumers",
    "children": [
        {
            "module": "printer",
            "properties": {
                "tag": "count",
                "format": "bracketed",
                "verbose": true
            }
        }
    ],
    "expose": { "inputs": { "numbers": "printer.value" } }
}
