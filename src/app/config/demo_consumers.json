{
    "group": "consumers",
    "modules": [
        {
            "module": "printer",
            "properties": {
                "tag": "demo",
                "format": "bracketed",
                "verbose": true
            },
            "config": "printing"
        }
    ],
    "expose": {
        "inputs": {
            "numbers": "printer.value",
            "labels": "printer.label",
            "measures": "printer.measure",
            "samples": "printer.sample",
            "probe": "printer.probe"
        }
    }
}
