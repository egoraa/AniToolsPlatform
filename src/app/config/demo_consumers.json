{
    "group": "consumers",
    "modules": [
        {
            "module": "printer",
            "properties": {
                "tag": "demo",
                "format": "bracketed",
                "verbose": true
            }
        }
    ],
    "expose": {
        "inputs": {
            "numbers": "printer.value",
            "labels": "printer.label",
            "measures": "printer.measure",
            "samples": "printer.sample",
            "probe": "printer.probe"
        }
    }
}
