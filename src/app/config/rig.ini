; A config the host does not parse, on purpose: the extension is not .json, so the bytes of this file
; reach the module verbatim and it parses them itself. See "config" of the scaler in demo.json.
rate = 48000
channels = 2
